# Tested compiler for this workspace. It is also the declared `rust-version`
# in Cargo.toml; bump both together and re-run the CPU/GPU gates and a perf
# comparison. rust-analyzer is optional editor setup (`rustup component add`).
[toolchain]
channel = "1.98.1"
profile = "minimal"
components = ["rustfmt", "clippy"]
