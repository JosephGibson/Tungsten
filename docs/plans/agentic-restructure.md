# Agentic restructure — instructions, tooling, dependencies

- **status:** in progress
- **goal:** Ship one coordinated restructure with smaller shared agent instructions, on-demand navigation, reproducible commands/checks, and current stable Rust dependencies.
- **non-goals:** New engine features; archive access/deletion; rewriting settled decisions; personal configuration/CLI updates; speculative performance tooling. Preserve behavior except necessary, reviewed migration changes.
- **files to touch:** See below.
- **ordered steps:** S0–S10, following the dependency table.
- **done-when:** All step checks and final acceptance checks pass; unavailable checks remain explicitly outstanding.

## Progress (handoff — keep accurate)

Execution started 2026-09-25 on `0.27` @ `6f80bb0`. Local logs: `perf-runs/agentic-restructure/<step>/` (gitignored, keep).

**Remaining (why status stays `in progress`):** CI green on the final revision (owner push); Metal/DX12 rendering and macOS/Windows audio on the upgraded stack; Windows skill symlinks. All S0–S10 steps are otherwise done; see Final acceptance.

**Resolved open questions (user, 2026-09-25):**

1. Pin 1.98.1 and set `rust-version = "1.98.1"`. Linux/Vulkan on the reference machine is the only required platform; Metal/DX12 remain outstanding, not claimed.
2. The reference machine (Radeon 660M / RADV / Mesa 26.2.3, PipeWire) supplies the visual, perf and audio baselines. Claude 2.1.110 and Codex 0.120.0 are the installed clients. Pause for the user to confirm audible and visual-by-eye checks.
3. Add CPU-only CI without branch protection and don't push. The "CI green" check stays outstanding until the owner pushes.
4. Approved: `cargo install --locked just@1.58.0 cargo-deny@0.20.2`; ShellCheck 0.11.0 release binary to `~/.local/bin`; headless `claude -p` / `codex exec` discovery checks; deleting tracked `.claudeignore` in S2.

**Deviations:**

- Work stays in the `0.27` working tree rather than a new branch (user decision after `git switch -c` was declined).
- Git is out of scope by user decision (2026-09-25): no branches, commits, tags or pushes, and no git-based checkpointing. Every change stays in the working tree for the owner. Checkpoints are working-tree checkpoints; files a risky change will touch are saved first to `perf-runs/agentic-restructure/<step>/backup/`, and a failed change is undone by hand (its manifest edits and matching `Cargo.lock` changes only).
- Context digest claim "Both skills' `../../../` repo links resolve one directory too shallow" did not reproduce: all original links resolved from `.claude/skills/<name>/` (checked 2026-09-25 against the S2 backup). Links were still re-checked through both access paths.
- Codex CLI upgraded 0.120.0 → 0.157.0 on 2026-09-25 by user instruction: 0.120.0 could no longer reach any model ("requires a newer version of Codex"). The personal `~/.codex/config.toml` was temporarily edited, then restored byte for byte. Revert with `npm i -g @openai/codex@0.120.0`. Claude Code stays at 2.1.110.

| Step | State | Notes | Check results |
| --- | --- | --- | --- |
| S0 | done | Env: reference machine (Arch, kernel 7.2.6-arch2-1, Ryzen 5 6600H, Radeon 660M RADV Mesa 26.2.3, Vulkan 1.4.354, X11 `:0`, PipeWire 1.6.9); rustc/cargo 1.95.0 (`stable`); `6f80bb0`, only untracked file this plan. Clients on PATH: Claude Code 2.1.110, Codex CLI 0.120.0 (this session runs through the Claude Agent SDK); nextest 0.9.133; just/cargo-deny/shellcheck/samply missing. Baseline PNG sha256 `acd3c434…5f00`, 1280×720; README provenance filled. Perf dirs `perf-runs/20260925T033414Z-ecs-high-load`, `…T033508Z…`, `…T033603Z…`; summary `perf-runs/agentic-restructure/S0-baseline.json`. Logs `perf-runs/agentic-restructure/S0/`. | `cargo fmt --all -- --check` exit 0. `cargo test --workspace --locked` exit 0: 626 passed, 0 failed, 4 ignored. `cargo clippy --workspace --all-targets --locked` exit 0 with 2 warnings (`physics_bench.rs:216` type_complexity, `:232` explicit_iter_loop). `WGPU_BACKEND=vulkan ./scripts/smoke-examples.sh` exit 0: 4/4 examples + MSAA×depth 4/4, post 2/2, SMAA 1/1, bloom 1/1, lighting 1/1. Baseline captured with the fixture README command (`WGPU_BACKEND=vulkan`, dev profile) exit 0; `test -s …/baseline-sprite-stress.png` passes. `TUNGSTEN_VISUAL_REGRESSION=1 WGPU_BACKEND=vulkan cargo test -p example-02-sprite-stress --test visual_regression --locked -- --nocapture` exit 0 (`sprite_stress_matches_baseline ... ok` on RADV). Perf `WGPU_BACKEND=vulkan ./scripts/perf-capture.sh ecs-high-load 300 --telemetry-only` ×3, all exit 0: median p95 total 89.38 ms, p95 acquire 0.10 ms, avg encode 0.38 / submit+present 0.18 / GPU 1.02 ms (update-bound scene, avg update 75.90 ms). **Audio: passed by ear (user, 2026-09-25 04:03Z), still on rustc 1.95.0 / pre-migration code.** `WGPU_BACKEND=vulkan RUST_LOG=info cargo run -p example-01-platformer`, cpal host ALSA `default` → PipeWire 1.6.9 → sink HDMI 2 ("Mi TV"), F32 / 44.1 kHz / 2 ch. Music loops and M toggles it, Space blip, right-click black-hole SFX, 1/2/3 volume presets and S stop all confirmed audible; no stream errors in `S0/audio-platformer-2.log`. An earlier attempt (`audio-platformer.log`) initialised cleanly but wasn't listened to. Missing-device behaviour not exercised. |
| S1 | done | Repo bytes (not tokens): Claude root startup `CLAUDE.md` 11 + `AGENTS.md` 5,757 = **5,768 B** (was 34,708 B with the mandatory index read); Codex root **5,757 B** (was 32,257 B). Render adds `crates/tungsten-render/AGENTS.md` 2,973 B (+ 11 B `CLAUDE.md` for Claude). `LLM_INDEX` 17,246 → 5,000 B and on demand; `DECISION_INDEX` 16,268 → 9,831 B with all IDs kept. User/system context, separate from repo bytes: `~/.claude/CLAUDE.md` 3,218 + `RTK.md` 964 + project `MEMORY.md` 462 = 4,644 B; no `~/.codex/AGENTS.md`. Plan conventions moved to `docs/plans/README.md`. Backups `perf-runs/agentic-restructure/S1/backup/`; logs `…/S1/`. | `wc -lc`: `AGENTS.md` 94 lines / 5,757 B (≤6,144, <200); `CLAUDE.md` 1 / 11 B (`@AGENTS.md` only); `crates/tungsten-render/AGENTS.md` 28 / 2,973 B (≤4,096) with a 1-line `@AGENTS.md` `CLAUDE.md`; `docs/LLM_INDEX.md` 67 / 5,000 B (≤8,192). `cargo test -p tungsten-core --test decision_index --locked` exit 0, 2 passed. All 112 index paths resolve (prefix-expanded check). **Claude Code 2.1.110** (`claude -p`, headless): (a) root launch, then reading `crates/tungsten-render/src/lib.rs`: root guidance present once plus render guidance (`perf-runs/agentic-restructure/S1/claude-root-lazy.json`). (b) Scratchpad canary experiment with `--output-format json` token deltas: `CLAUDE.md`→`@AGENTS.md` loads the body once (+1,375 tokens for a 4,642 B file, not doubled); a bare `AGENTS.md` isn't loaded natively by 2.1.110; launching from a subdirectory loads the nested file but skips the ancestor's `@AGENTS.md`, because it's an external import (`hasClaudeMdExternalIncludesApproved=false` for this project, so headless runs can't approve it). Render-dir launch therefore lacks root rules until the one-time interactive approval; documented in S2. The model's self-report of a duplicated root file contradicted the token evidence and was discarded. **Codex:** 0.120.0 exited 1 before any model call: the personal `service_tier = "default"` was rejected, it couldn't decode the model catalog, and `-c service_tier="flex"` got HTTP 400. With `service_tier` commented out, every model (`gpt-6-astra`, `gpt-5.5`) returned "requires a newer version of Codex". Per user instruction ("try again and if not fix"), upgraded to **Codex CLI 0.157.0** (`npm i -g @openai/codex@0.157.0`) and restored the original config byte for byte, so the net personal-config change is zero. `codex exec --sandbox read-only` from root: root `AGENTS.md` ×1. With `-C crates/tungsten-render`: root ×1 + render ×1, no duplicate root body. Confirmed objectively in the session rollouts, where each body appears in exactly one model-visible `response_item/message` and the render body is absent from the root session. |
| S2 | done | `.gitignore` `.claude/` blanket replaced by an allowlist (`settings.json` + the two skill dirs). New `.ignore`, `.claude/settings.json` (Glob `.gitignore` option + `Read(./docs/plans/archive/**)` deny, no allows), `.agents/skills/{tungsten-wgpu,tungsten-perf}` symlinks, `docs/agent-setup.md`. Skills fixed: wgpu shader facts per D-057–D-061, Context7 requirement replaced with versioned docs.rs/upstream sources, D-015 wording, `display.present_mode` precedence; perf canonical scene `ecs-high-load`, secondary `sprite-stress`/baseline. `.claudeignore` deleted (backup `perf-runs/agentic-restructure/S2/backup/`). Effective settings inspected read-only: user settings have 86 allows + git-mutation denies and no `env`; `settings.local.json` has 22 allows + a PreToolUse guard (both untouched); no managed settings. | `git check-ignore -v .claude/settings.local.json .claude/hooks/guard.sh` exit 0 (`/.claude/*`); `git status` shows `.claude/settings.json` and both `SKILL.md` as trackable. `test -f .agents/skills/tungsten-wgpu/SKILL.md` and the perf equivalent pass. Links: wgpu 4 and perf 3 resolve both lexically via `.agents/…` and via realpath. Descriptions 221/235 chars (≤300), bodies 3,860/4,366 B (≤8,192). `jq -e . .claude/settings.json` exit 0. `rg --files --hidden -g '!.git/**' -g '!docs/plans/archive/**'`: 466 files, 0 under `target/` or `perf-runs/`, 0 `settings.local`; finds 4 manifests, 67 `.wgsl`, `Cargo.lock`, the baseline PNG and 2 `SKILL.md` (symlinks not followed). The archive exclusion was inspected in `.ignore`, never scanned. Claude 2.1.110 (`claude -p`, repo root): Glob found 4 manifests, 67 WGSL and 0 `target/**/*.d`; Grep `STRESS_SCENE` hit no generated paths; the Skill tool invoked both skills and returned the updated content (`ecs-high-load`, `crates/tungsten-render/AGENTS.md`). Scratch test: Glob honours `.gitignore` only with `CLAUDE_CODE_GLOB_NO_IGNORE=false`; Grep honours `.gitignore` and `.ignore` but not `.claudeignore`. Codex CLI 0.157.0 (`codex exec --sandbox read-only`, root) listed both repo skills at `.agents/skills/{tungsten-perf,tungsten-wgpu}/SKILL.md`, read them through the symlinks, followed their links to `docs/perf/profiling-workflow.md` and `crates/tungsten-render/AGENTS.md` without errors, and returned `ecs-high-load` / `crates/tungsten-render/AGENTS.md` (`S2/codex-skills.txt`). Windows symlink prerequisites documented, not tested (no Windows host). |
| S3 | done | `rust-toolchain.toml` pinned from `stable` to `1.98.1` (minimal profile + rustfmt/clippy). Workspace `rust-version = "1.98.1"`, inherited by all 7 members (examples keep their hard-coded edition until S5). `.cargo/config.toml` comments corrected: bundled LLD since 1.90 replaces the stale Arch 1.94 note, and the `RUSTFLAGS` precedence is documented and verified with a scratch probe (even an empty `RUSTFLAGS` replaces target flags); `target-cpu=native` kept. `rustup toolchain install 1.98.1` also set a global rustup default (none existed before); reverted with `rustup default none`, so the repo resolves via the toolchain file only. Stable 1.95.0 stays installed. Lock unchanged. Backups `perf-runs/agentic-restructure/S3/backup/`. | `rustc --version` → `rustc 1.98.1 (48a229cea 2026-09-01)`. `cargo metadata --no-deps --format-version 1 --locked \| jq -e 'all(.packages[]; .rust_version == "1.98.1")'` → `true`. `cargo fmt --all -- --check` exit 0. `cargo test --workspace --locked` exit 0: 632 passed (626 + 6 S6), 4 ignored. `cargo clippy --workspace --all-targets --locked` exit 0, advisory, new on 1.98.1: 8× `manual_midpoint` (`tungsten-core/src/tween.rs:134,144`, `tungsten/src/audio.rs:121,140`, `examples/04_shader_playground/src/main.rs:452,466`, `tungsten-core/tests/substep_probe.rs:40`, `physics_determinism.rs:33`, `physics_containment.rs:34`, `physics_bench.rs:47`, `examples/01_platformer/src/tests/main.rs:770-771`) and 8× `chunks_exact_to_as_chunks` (`tungsten/src/asset_loader.rs` ×6, `tungsten-render/src/renderer.rs:1705`, …), plus the 2 baseline bench warnings. A new `semicolon_if_nothing_returned` in the S6 test was fixed; the rest are left for S4 without suppression. `cargo bench --workspace --no-run --locked` exit 0 (13 executables). `WGPU_BACKEND=vulkan ./scripts/smoke-examples.sh` exit 0 (4/4 + 4/4 + 2/2 + 1/1 + 1/1 + 1/1). Visual test exit 0 against the S0 PNG (`sprite_stress_matches_baseline ... ok`). Perf ×3 `ecs-high-load 300 --telemetry-only` (dirs `perf-runs/20260925T043413Z-ecs-high-load`, `…T043623Z`, `…T043718Z`), compiler-only vs S0 medians: p95 total 88.94 vs 89.38 ms (−0.5%), p95 acquire 0.09 vs 0.10 ms (−0.01 ms absolute, noise-level baseline), encode 0.38/0.38, submit+present 0.18/0.18, GPU 1.02/1.02; avg update +1.0%. No regression. Same script flags as S0. |
| S4 | done | Installed (pre-approved): `cargo install --locked just@1.58.0 cargo-deny@0.20.2` (built with 1.98.1); ShellCheck 0.11.0 release tarball verified against the GitHub asset digest `sha256:8c3be12b…7198`, installed to `~/.local/bin`. New `justfile` (fmt, fmt-check, lint, test, check, bench-build, smoke, visual, perf, perf-test, deps, ctx, plus `script-test`; `positional-arguments`, no interpolation), `deny.toml`, `scripts/check-agent-context.py` (stdlib, with `--self-test`), `scripts/test-smoke-examples.sh`. `smoke-examples.sh` refactored into `run_row`: discovery fails on metadata/jq errors or zero examples; `--preserve-status` dropped (it made timeouts exit 143, so the 124 branch never fired); `--kill-after=10`; matrix unchanged. `perf-capture.sh`: explicit `TUNGSTEN_PERF_RUSTFLAGS` (default unchanged `-C force-frame-pointers=yes`, so baselines stay comparable), compiler and flags recorded in README, flamegraph folded from the run's own `perf-record.data` (`flamegraph --perfdata`) instead of `cargo flamegraph`, which rebuilt `target/release` with native/no-frame-pointer flags that perf then profiled, and left `perf.data` in the repo root. Clippy fixed on 1.98.1: `f32::midpoint` at 10 sites, `as_chunks[_mut]::<4>()` at 7, bench `Scenario` alias + `*counts`, and two scoped `#[expect(clippy::manual_midpoint, reason)]` on symmetric tween arms. `AGENTS.md` commands, workflow doc and setup doc updated. Nextest not adopted (optional). Pre-existing loose root `perf.data` (328 MB, 2026-07-13), `perf.data.old` and `flamegraph.svg` (2026-04-22) left untouched. Backups `perf-runs/agentic-restructure/S4/backup/`. | `just --list` OK. `just check` exit 0 (fmt-check, `clippy -D warnings`, tests 632 passed / 4 ignored, 0 doctests). `just bench-build`, `just deps`, `just ctx` (6,102 B root; self-test 9/9), `just perf-test`, `just script-test` (`shellcheck scripts/*.sh` clean + `bash scripts/test-smoke-examples.sh` OK) all exit 0. Smoke regression tests run against the old script fail 4 cases (metadata failure, bad JSON, zero examples, timeout label), proving they catch the fixed bugs; the matrix-row diff passes on both, so the matrix is preserved. `WGPU_BACKEND=vulkan just smoke` exit 0: 13/13 rows, same matrix. `just visual` exit 0 after the screenshot-path `as_chunks_mut` change. Forwarding: `just test -p tungsten-core --test decision_index` ran only that test; the filter `'no such test; echo pwned'` stayed one argument (0 run, 2 filtered, nothing echoed); `just test -p no-such-package` exited 101 with the full cargo error. Full capture `perf-runs/20260925T044305Z-ecs-high-load`: telemetry, `perf-stat.txt`, 947 MB `perf-record.data` and `flamegraph.svg` all in the capture dir; root `perf.data` size and mtime unchanged; README records `rustc 1.98.1` and `-C force-frame-pointers=yes`; `RUSTFLAGS="-C force-frame-pointers=yes" cargo build --release -p example-02-sprite-stress` is a no-op, so the flags match the binary. **cargo-deny baseline:** licenses/bans/sources ok. Advisories got 6 temporary reasoned ignores: RUSTSEC-2026-0190 (anyhow), -0204 (crossbeam-epoch), -0274 (rtrb) → S5; -0194/-0195 (quick-xml via winit/wayland-scanner, build time) → revisit S5; -0192 (ttf-parser unmaintained via cosmic-text 0.18) → revisit S9. 29 duplicate-version warnings. |
| S5 | done | **Target recheck (2026-09-25, crates.io `max_stable_version`, non-yanked):** every target matches the table except **rtrb**. 0.4.0 exists (published 2026-08-17, before the plan's lookup), so the "0.4.0 was not found" note was wrong; the S5 target stays 0.3.5 (0.4 is in Follow-ups). Rust stable is still 1.98.1 (channel manifest 2026-09-03). Tools unchanged: just 1.58.0, cargo-deny 0.20.2, nextest 0.9.146, cargo-shear 1.14.0, cargo-machete 0.9.2. **Deps checkpoint:** `cargo update` (compatible only), 250 version lines changed, sources still crates.io + the glyphon git pin; rtrb floor raised to `0.3.5`. Notable: anyhow 1.0.104, rtrb 0.3.5, crossbeam-epoch 0.9.21, quick-xml 0.41.0 (wayland-scanner 0.31.11), wgpu/naga 29.0.1 → 29.0.4, serde 1.0.229, serde_json 1.0.151, thiserror 2.0.21, log 0.4.34, env_logger 0.11.11, bytemuck 1.25.2. New transitive crates include skrifa/read-fonts, syn 3.0.6, getrandom 0.4.3, zlib-rs, defmt. Removed 5 resolved deny ignores (0190, 0194, 0195, 0204, 0274); RUSTSEC-2026-0192 remains until S9. Backup `perf-runs/agentic-restructure/S5/backup-deps/`. **Edition checkpoint** (backup `S5/backup-edition/sources.tar`): `cargo fix --edition --workspace --all-targets --allow-dirty` on 2021 made 2 fixes. `physics/step.rs` got the 2024 pattern fix `|&(&k, _)|`, kept. `display.rs` `if_let_rescope` rewrote an `if let/else` to `match`; reverted to the original because the scrutinee is a plain `Option` with no side-effecting `Drop`. Fourteen `tail_expr_drop_order` warnings reviewed (`app.rs` ×6, `asset_loader.rs` ×7, `state.rs:140`): they involve `anyhow`/`image` error values or moved-from `Option<Box<dyn GameState>>` temporaries; no locks or guards, and no `GameState` implements `Drop` (the repo's only `Drop` impl is a test's `TempPng`), so no code change. Then workspace `edition = "2024"`, `resolver = "3"`; the 4 examples now use `edition.workspace = true`. Resolver 3 left `Cargo.lock` byte-identical. On 2024, clippy `collapsible_if` flagged 53 nested `if let` sites (let-chains); fixed with `cargo clippy --fix` limited to that lint (23 files) rather than allowing it. Env-mutation audit (`crates/tungsten-core/tests/display.rs`): every env access in that binary goes through `std::env` (std's lock), there's no FFI/libc in core, and the only reader test holds `ENV_LOCK`; SAFETY comments added. No platform-specific `cfg` code exists, so the host-only migration missed nothing, and no migration TODO remains. **Format checkpoint:** new `rustfmt.toml` `style_edition = "2024"` for direct rustfmt callers; `cargo fmt --all` reformatted 63 files. Checked against a pre-format snapshot: 61 files keep an identical non-whitespace character multiset; the other two only gained a trailing comma (test tuple) and `;` after a diverging `return Err(…)` (`config.rs:407`). | Deps checkpoint: `just check` exit 0 (632/0/4, strict clippy clean). `just bench-build` exit 0. `just deps` exit 0 with no unused-ignore warnings. `WGPU_BACKEND=vulkan just smoke` exit 0 (13/13). `just visual` exit 0 (pixel match vs the S0 PNG). Perf ×3 (`perf-runs/20260925T045752Z`, `…T050002Z`, `…T050058Z`) vs S3 medians: p95 total 91.11 vs 88.94 ms (+2.4%, inside run spread 86–96 ms), p95 acquire 0.09/0.09, encode 0.41 vs 0.38 ms (+0.03 ms), submit+present 0.18/0.18, GPU 1.03/1.02. No >10% regression. **Edition + format checkpoint:** `cargo metadata --no-deps --format-version 1 --locked \| jq -e 'all(.packages[]; .edition == "2024")'` → `true`. `cargo test --workspace --locked` 632/0/4 both before and after the clippy fix. `just check` exit 0 (fmt clean, `clippy -D warnings` clean, 632/0/4). `just bench-build` exit 0. `WGPU_BACKEND=vulkan just smoke` exit 0 (13/13). `just visual` exit 0 (pixel match). Perf ×3 (`perf-runs/20260925T050729Z`, `…T050900Z`, `…T050957Z`) vs deps checkpoint: p95 total 87.53 vs 91.11 ms (−3.9%), p95 acquire 0.09/0.09, encode 0.39/0.41, submit+present 0.19/0.18, GPU 1.02/1.03. No regression. |
| S6 | done | New `crates/tungsten-render/tests/shader_coverage.rs` (std only, rooted at `CARGO_MANIFEST_DIR`): discovers `.wgsl` under `crates/tungsten-render/src` and `assets/shaders`, Naga-validates each through `validate_wgsl_source`, checks `shaders/stock/**` ↔ `stock/**` and `sprite.wgsl` both ways byte for byte, and fails on empty discovery or an empty mirror set. It found `lygia/noise.wgsl` (both copies) isn't standalone WGSL: it calls `hash12` from sibling `hash.wgsl`. The LYGIA files are vendored reference snippets never compiled by the engine, so the test prepends declared siblings via an explicit `FRAGMENT_DEPS` table. The 7 existing per-shader Naga tests and the mirror/layout tests are kept. Render `AGENTS.md` points at the test. | `cargo test -p tungsten-render --locked` exit 0: 79 unit + 6 `shader_coverage` passed (rustc 1.95.0). `-- --nocapture` reports **67 paths, 36 distinct contents, 31 mirror pairs**, matching the plan. Disposable-copy negatives are permanent tests on a fresh `CARGO_TARGET_TMPDIR` tree: malformed WGSL, orphan asset mirror, orphan engine copy, mismatched bytes and empty asset tree each fail with the expected message; a valid 4-file fixture passes (4/1/2). Naga success isn't GPU/pixel evidence. |
| S7 | done | **notify 6.1.1 → 8.2.0** (`version = "8.2"`, `default-features = false` kept: no `macos_fsevent`, and 8.x has no crossbeam feature, so std mpsc intent holds). No source change needed. Lock: inotify 0.11.5, mio 1.2.3, + notify-types 2.1.0, windows-sys 0.60 for notify, − filetime. Backup `perf-runs/agentic-restructure/S7/notify/backup/`. **pollster 0.4.0 → 1.0.1** (`"1.0"`): the two `pollster::block_on(<future>)` calls in `renderer.rs` compile unchanged. **criterion 0.5.1 → 0.8.2** (`"0.8"`, `html_reports` kept; no async feature enabled) in core/render/umbrella dev-deps; the six benches now `use std::hint::black_box` instead of the deprecated `criterion::black_box`. Lock: criterion-plot 0.8.2, + alloca/page_size/winapi, − itertools 0.10/is-terminal. Backups under `S7/{pollster,criterion}/backup/`. **glam 0.32.1 → 0.33.10** (`"0.33.10"`, default type features kept). Only deprecation: `Mat4::orthographic_rh` (0.33.2 moved camera constructors into `glam::camera`), at 5 sites (core `camera.rs` ×2, `renderer.rs:988`, two camera tests). Migrated to `glam::camera::rh::proj::directx::orthographic` (doc alias `webgpu`: right-handed, Z in [0, 1], Y-up), the replacement named by glam's deprecation note. Checked in source that it's arithmetically identical: `zz = −1/(far−near)` vs `1/(near−far)`, `tz = −near/(far−near)` vs `near/(near−far)`; IEEE negation/division are sign-symmetric; x/y/translation unchanged. Handedness and depth conventions preserved. Dropped an unused `Mat4` test import. Backup `S7/glam/backup/`. | notify: `just check` exit 0 (632/0/4), `just bench-build` exit 0, `just deps` exit 0 (licenses ok incl. CC0), `WGPU_BACKEND=vulkan just smoke` exit 0 (13/13), `just visual` exit 0. Behavior probe `perf-runs/agentic-restructure/S7/notify/hot-reload-probe.sh` (local) against `example-01-platformer`, the only example that enables hot reload (a first run against the playground had no watcher and was discarded). In-place body edit of `assets/shaders/stock/vignette.wgsl` → `shader 'vignette' reloaded`. Invalid WGSL → `shader 'vignette' validation failed … WGSL parse failed` with the app still running. Editor-style rename-save (temp + `mv`) of valid content → reloaded again (recovery). Rename-save of `assets/animations/walk.json` → `Hot-reloaded animation 'walk'`. Originals restored byte for byte (sha256 equal), no temp files left, `git status assets/` clean. pollster: `just check` 632/0/4, `just bench-build`, `just deps`, `just smoke` (13/13) and `just visual`, all exit 0. criterion: the same gates all exit 0 (strict clippy clean). Fresh baselines: `cargo bench --workspace --locked --bench '*' -- --save-baseline agentic-criterion-0.8 --noplot` exit 0, 33 benchmarks (`target/criterion/*/agentic-criterion-0.8`, summary `perf-runs/agentic-restructure/S7/criterion/baseline-summary.txt`). A first attempt without `--bench '*'` failed because examples' libtest harnesses reject `--save-baseline`. This is a new harness baseline, not compared with 0.5 numbers and not presented as an engine speedup; physics `dense_pile/10000` and `pile_plus_bullet/10000` have wide intervals (sleep/wake dynamics). glam: `just check` exit 0 (632/0/4, `clippy -D warnings` clean). `just bench-build` exit 0. `cargo test --release -p tungsten-core --test physics_determinism --test physics_tunneling --test physics_containment --locked` exit 0 (1 + 2 + 1 passed). `just deps` exit 0. `just smoke` exit 0 (13/13). `just visual` exit 0 (pixel match vs the S0 PNG). Perf ×3 (`perf-runs/20260925T053105Z`, `…T053240Z`, `…T053335Z`) vs S5 edition: p95 total 85.38 vs 87.53 ms (−2.5%), p95 acquire 0.09/0.09, encode 0.36/0.39, submit+present 0.18/0.19, GPU 1.02/1.02. No regression. |
| S8 | done | **Decoder coverage first:** new `crates/tungsten-core/tests/audio_decode.rs` (7 tests) + synthetic fixtures `crates/tungsten-core/tests/fixtures/audio/` (0.25 s 440 Hz tone, left channel only, ~53 KB, FFmpeg n9.0.1; commands and provenance in its README; project-generated, MIT). Covers WAV, Ogg/Vorbis mono, MP3, AAC/ADTS: rate/channels, whole frames, frame-count range, finite PCM in [−1, 1], peak, L/R interleaving via right-channel RMS, half-truncated MP3 → prefix, truncated Ogg (header and audio cuts) → error, garbage/empty/missing → error. **Found on 0.5 (pre-existing, not fixed):** PCM WAV can't decode, because the workspace enables Symphonia `wav` but not `pcm` ("unsupported codec"). No shipped asset is WAV; the test pins the current error and the fix is in Follow-ups. **Symphonia 0.5.5 → 0.6.1** (`"0.6"`, same features, `default-features = false`, so `all-meta`/ID3 stays off; all shipped sounds are Ogg): `probe(&hint, mss, opts, opts)`, first track whose `codec_params.audio()` isn't `CODEC_ID_NULL_AUDIO`, `make_audio_decoder`, `next_packet() → Ok(None)` at EOF, `copy_to_vec_interleaved` into a reused scratch vec (it resizes, it doesn't append), `packet.track_id` is now a field. I/O policy: `UnexpectedEof` = truncated, keep prefix + warn; other `IoError` is now an error (0.5 treated every I/O error as EOF); `DecodeError` packets skipped with a warning. Lock: + symphonia-common, num-complex, regex-lite; − symphonia-utils-xiph, encoding_rs. Backup `perf-runs/agentic-restructure/S8/symphonia/backup/`. **cpal 0.15.3 → 0.18.2** (`"0.18"`, default features, which are empty in 0.18: no pipewire/pulseaudio/jack/realtime, so Linux keeps the ALSA host and backend selection is unchanged; `.play()` kept). `crates/tungsten/src/audio.rs`: `device.name()` → `device.description()` → `DeviceDescription::name()`, `sample_rate().0` → `sample_rate()` (u32), `build_output_stream(config.into(), …)` by value; unified `cpal::Error` formats through the existing `map_err`/error callback. Lock: alsa 0.11, coreaudio-rs 0.14 + objc2 audio crates; − oboe/ndk/jni/bindgen/clang-sys and old windows 0.54. Backup `S8/cpal/backup/`. Untested platforms: macOS CoreAudio and Windows WASAPI (no hosts available). | Decode tests, same test code: 7/7 on 0.5 (`S8/decode-pre-symphonia05.log`) and 7/7 on 0.6.1 (`S8/decode-post-symphonia06.log`). Deltas: MP3 12,672 → 11,025 frames (0.6 trims encoder delay/padding, exact nominal), Ogg 5,760 → 5,513, AAC 13,312 unchanged, truncated MP3 4,608 → 3,503, truncated-Ogg message "end of stream" → "unexpected end of file". Peaks, rates, channels and interleaving unchanged. Symphonia checkpoint: `just check` exit 0 (639/0/4 = 632 + 7), `just bench-build`, `just deps` (licenses ok), `just smoke` (13/13) and `just visual` all exit 0. Perf ×3 (`perf-runs/20260925T055620Z`, `…T055802Z`, `…T055854Z`) vs glam: p95 total 77.20 vs 85.38 ms (−9.6%), GPU 1.07 vs 1.02 ms. The decoder isn't on this scene's frame path, so this is run/thermal variance, not a speedup. **Audible (user, 2026-09-25 ~06:00Z):** `cargo run -p example-01-platformer`, cpal `default` → PipeWire, F32 / 44.1 kHz / 2 ch. Music loop (padding now trimmed), M toggle, Space blip, black-hole SFX, 1/2/3 volume, S stop all confirmed correct; no decode warnings in the log. **Missing device:** child-only `ALSA_CONFIG_PATH=<empty file>` → ALSA `Unknown PCM default`, engine logs `Audio init failed (continuing without audio): Failed to get output config …` and the 30-frame smoke run exits 0. cpal checkpoint: `just check` exit 0 (639/0/4, strict clippy clean), `just bench-build`, `just deps`, `just smoke` (13/13) and `just visual` all exit 0. Perf ×3 (`perf-runs/20260925T061312Z`, `…T061430Z`, `…T061518Z`) vs Symphonia: p95 total 70.06 vs 77.20 ms, avg update 65.25 vs 71.68 ms, GPU 1.02 vs 1.07. Neither upgrade is on the update path, so this is unattributed variance, not claimed as a gain. No regression. **Host/device change:** cpal 0.18 opens `Default Audio Device` at **48 kHz** (0.15 opened `default` at 44.1 kHz), so the engine's load-time resampler (`prepare_pcm`) now converts the 44.1 kHz sounds. **Audible (user, 2026-09-25 ~06:16Z):** all controls correct, same pitch and tempo, no crackle; no stream errors in `S8/cpal/audio-platformer.log`. **Missing device (cpal 0.18):** same empty `ALSA_CONFIG_PATH` → `Audio init failed (continuing without audio): Failed to get output config: The requested audio device is not available…`, exit 0. |
| S9 | done — Metal/DX12 outstanding | **wgpu 29.0.4 → 30.0.1 with glyphon git `5ac0224` (0.11) → crates.io 0.12.0** in one checkpoint (no intermediate 0.11 migration); cosmic-text 0.18.2 → 0.19.0, naga 30.0.1 (+ naga-types). Ports: `Some(..)` vertex buffer slots (quad, debug_line, sprite, and `SpritePipeline::vertex_layouts()` for lit_sprite/material); `SurfaceConfiguration.color_space: SurfaceColorSpace::Auto` (SDR kept, HDR deferred); `RequestAdapterOptions.apply_limit_buckets: false` (native, real limits); `Queue::present(output)`; fallible `get_mapped_range()`: screenshot → `ScreenshotError::MapFailed`, GPU-timing readback always unmaps after a successful map and warns on a view failure; cosmic-text 0.19 `Buffer::set_size`/`set_text` without `FontSystem` (shaping stays in `shape_until_scroll`). No shader edits needed: naga 30 validates all 67 WGSL (integer varyings already flat). **Surface acquire:** new `crates/tungsten-render/src/surface_acquire.rs`, a pure policy: Success → render; Suboptimal → render, then reconfigure after `queue.present` releases the texture (resizing if the window size changed); Timeout/Occluded → skip frame; Outdated → reconfigure + one retry, then skip; Lost → recreate the surface from a stored `Arc<Window>` + one retry, then fail with a clear "device recovery is not supported" error; Validation → fail. At most 2 acquires per frame, and no texture is held while configuring. 7 transition tests in `src/tests/surface_acquire.rs`. `deny.toml`: `allow-git = []`, and RUSTSEC-2026-0192 turned into a justified standing exception (ttf-parser is still reached via cosmic-text 0.19 → fontdb 0.23; no fixed release). Backup `perf-runs/agentic-restructure/S9/backup/sources.tar`. | `just check` exit 0: 646 passed (639 + 7), 4 ignored, `clippy -D warnings` clean, `tests/shader_coverage.rs` passes under naga 30. `just bench-build` exit 0. `just deps` exit 0 (`unknown-git = "deny"` with an empty allow-list). `! rg -q 'git\+' Cargo.lock` passes. `cargo tree --locked -i wgpu` → only `wgpu v30.0.1`, no duplicate wgpu. `WGPU_BACKEND=vulkan just smoke` exit 0, 13/13 incl. MSAA/depth, post-stack, SMAA, bloom and lighting fixtures. `just visual` exit 0: **pixel match** vs the S0 PNG (no differences to explain; covers the screenshot readback path). Hot-reload probe on wgpu 30 (`S9/reload-probe.txt`): shader reload, invalid-WGSL error + recovery, rename-save and animation reload all ok, files restored. GPU timing readback checked separately in each capture's `TUNGSTEN_GPU_TIMING=1` run: 365 numeric `gpu=` lines per run, 0 `n/a`. **By-eye (user, 2026-09-25 ~13:50Z)**, platformer + `TUNGSTEN_LIGHTING_FIXTURE=on`: outlined text, F4 HUD, F1–F3 overlays, lit sprites, drag-resize, minimize/restore and maximize all correct; no surface warnings or errors in `S9/eye-platformer.log`. **Perf:** a first ×3 set (`…T134235Z`, `…T134431Z`, `…T134523Z`) was confounded by NoMachine screen encoding (`nxcodec.bin` ~34% CPU from ~13:33Z): +10.6% p95, all in the update stage wgpu doesn't touch. Discarded. After the user disconnected (98.8% idle), ×3 (`perf-runs/20260925T135050Z`, `…T135138Z`, `…T135225Z`) vs cpal: p95 total 69.99 vs 70.06 ms (−0.1%), update 65.02 vs 65.25, GPU 1.02/1.02, p95 acquire 0.05 vs 0.06; encode +0.05 ms (0.38 vs 0.33, run spread 0.33–0.40) and submit+present +0.03 ms (0.19 vs 0.16) are small absolute increases, possibly wgpu 30's queue-owned present, ~0.1% of the frame. No frame-time regression. **Metal/DX12: outstanding** (no macOS/Windows hosts; Vulkan success isn't certification). |
| S10 | done — CI green outstanding (owner push) | New `.github/workflows/ci.yml`: CPU-only, triggered by `pull_request`, `workflow_dispatch` and push to `main`/`0.*`, `permissions: contents: read`, concurrency cancel-in-progress. Actions pinned to reviewed SHAs: `actions/checkout@3d3c42e…` (v7.0.1, latest), `actions-rust-lang/setup-rust-toolchain@ecabd13…` (v2.0.0, reads `rust-toolchain.toml`; its rust-cache v2.9.2 keys on rustc, `Cargo.lock` and `RUST*` env). v2.0.0 defaults `build-warnings` to `deny`; CI sets `""` so `just lint` stays the warning gate, as locally. Job `RUSTFLAGS: "-C target-cpu=x86-64"` is the generic-CPU override (changed from `""` in the QA pass; same codegen, but can't be lost as an empty value). Tools: ShellCheck 0.11.0 tarball + SHA-256; `cargo install --locked just@1.58.0 cargo-deny@0.20.2`. Linux packages start at pkg-config, libasound2-dev, libudev-dev. Steps: `just check`, `bench-build`, `deps`, `ctx`, `script-test`. No branch protection or GPU/perf thresholds. Policy decisions appended: D-068 (agent instructions/skills), D-069 (toolchain, edition, gates), D-070 (CPU-only CI, narrows D-002), with index rows. `AGENTS.md` "Not doing" updated for CI (6,131 B). README (docs table, Quick Start toolchain/just, agent read order; version/branch status line left for release) and CHANGELOG `[Unreleased]` describe the delivered scope. | Workflow YAML parses (`python3 yaml.safe_load`; actionlint not installed). **Local CI simulation** (fresh `CARGO_TARGET_DIR=target/ci-sim`, `RUSTFLAGS=""`, `CARGO_INCREMENTAL=0`): `just check` (646/0/4), `bench-build`, `deps`, `ctx`, `script-test` all exit 0. **Final state (default target, native flags):** `cargo fmt --all -- --check` exit 0; `cargo test --workspace --locked` exit 0, 646 passed / 0 failed / 4 ignored; `just lint`, `just deps`, `just ctx`, `just script-test` exit 0; `cargo metadata … \| jq -e 'all(.packages[]; .edition == "2024" and .rust_version == "1.98.1")'` → `true`; `! rg -q 'git\+' Cargo.lock` passes; `cargo test -p tungsten-core --test decision_index` passes with D-068–D-070; `git diff --check` exit 0 and no trailing whitespace in the 26 untracked files. `git status --short`: 96 modified, 1 deleted (`.claudeignore`, approved), 26 untracked, all intended. No Rust source changed after the S9 GPU/audio/perf gates. **CI green: outstanding** until the owner pushes; the first run also confirms the Linux package list. |

## Post-completion cleanup and QA (2026-09-25, user-requested)

**Cleanup:** deleted the loose, gitignored, untracked `perf.data` (328 MB, 2026-07-13), `perf.data.old` (219 MB, 2026-07-08) and `flamegraph.svg` (2026-04-22) from the repo root, plus `target/ci-sim` (2.9 GiB, via `cargo clean --target-dir target/ci-sim`). The main `target/` (161 GB), `target/criterion` baselines, `perf-runs/` captures and these logs were kept.

**QA findings, all fixed** (backup `perf-runs/agentic-restructure/QA/backup/`):
1. `Suboptimal` reconfigured every frame. A platform that keeps reporting a surface as suboptimal at an unchanged size would have recreated the swapchain each frame. New pure `reconfigure_for_suboptimal`: always on a size change, once per unchanged size until the next clean `Success`. 2 new tests.
2. The `Outdated` retry reconfigured at the stale config size, so with a pending resize event it just got `Outdated` again. It now uses `sync_surface_to_window` (the window's current size, ignoring zero/minimized).
3. The CI `RUSTFLAGS: ""` relied on an empty env value surviving the runner and the setup action's `-v` check. It's now the explicit `-C target-cpu=x86-64` (rustc's default for the target). D-070 text and the `.cargo/config.toml` comment updated.
4. The `D-002` index row didn't point at `D-070`; annotated. Docs updated to match: CHANGELOG, `docs/agent-setup.md` archive-deny caveat, `LLM_INDEX` rows for `surface_acquire.rs` and `audio_decode.rs`, and a render `AGENTS.md` note that the acquire policy lives in `surface_acquire.rs`. The recreate-surface drop order is now documented in code.

**Checked, no issue:** the let-chain rewrites (`collapsible_if` fires only without an `else`; the `else if` sites and the four chains in `config.rs` were reviewed against the pre-edition backup); `as_chunks` and `f32::midpoint` rewrites; the Symphonia/cpal/wgpu/glyphon ports; the screenshot readback error path (per-capture buffer, dropped after use); every doc link in README/DESIGN/CHANGELOG/setup/fixture READMEs; stale version statements (the remaining ones are intentional history). `cargo doc --workspace --no-deps` with `-D rustdoc::broken_intra_doc_links`: exit 0. Smoke at `RUST_LOG=warn`: **zero** warnings or errors across all 13 rows; a positive control (forced audio failure) printed its WARN at the same level. The Khronos validation layer isn't installed (not added; would need a system package), so wgpu-core validation plus naga are the GPU validation used. Resize/minimize/restore driven through libX11 (`QA/xwindow_probe.py`: 900×600, 1400×800, 640×360, iconify, restore, 1920×1080): frames kept advancing through every step, no surface warnings, errors or panics; resizes took effect. Acquire branches aren't logged, so this shows end-to-end behavior, not which branch ran; on X11 a minimized window keeps acquiring, so `Occluded` wasn't hit. `.claude/settings.json` archive deny: a scratch canary test showed both `Read(./…)` and `Read(/…)` deny at the project root; from a subdirectory of a non-git scratch project the parent settings didn't apply (not verifiable without `git init`, which the user's settings deny).

**Final gates after QA:** `just check` (648 passed / 0 failed / 4 ignored), `just bench-build`, `just deps`, `just ctx`, `just script-test`, `WGPU_BACKEND=vulkan just smoke` (13/13) and `just visual` (pixel match) all exit 0. **Perf not re-run:** NoMachine was encoding the display again, which confounded S9's first set. The QA change adds one enum comparison per frame on the normal path.

## Context digest

Reviewed 2026-09-24–25 at `0.27` / `6f80bb0`, workspace `0.26.0`: 626 tests passed, four ignored, formatting clean, two clippy warnings.

Keep `AGENTS.md` canonical with a one-line Claude import. Shrink the existing index, scope renderer instructions, and share the two repaired skills. Preserve decision/design/changelog paths and archived plans. Add search filtering, a small context check, `just`, dependency checks and CPU-only CI; nextest remains optional.

Pin Rust 1.98.1; migrate all seven packages to edition 2024 / resolver 3. Upgrade dependencies in separately verifiable working-tree checkpoints, pairing wgpu/glyphon. Correct `rtrb` target: 0.3.5, not 0.4.0. Preserve audio backend features. Establish shader coverage and the missing visual reference PNG before migrations.

Work in the `0.27` working tree with rollback checkpoints. Cargo edits are serial; independent docs/tests/tooling may overlap. Preserve comparison flags and require real GPU/audio verification. Budgets below are proposed project limits, not product limits. Never read `docs/plans/archive/`.

## Verified baseline

Repo evidence was read directly. Review commands: `cargo test --workspace --locked`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked`; all exited 0. No migration, formatter write, installation, smoke run or perf capture was executed.

| Evidence | Verified state |
| --- | --- |
| `AGENTS.md`, `CLAUDE.md`, `docs/LLM_INDEX.md` | 15,011 B / 154 lines; 2,451 B / 28 lines; 17,246 B / 78 lines |
| `DECISIONS.md`, `docs/DECISION_INDEX.md` | 103,985 B / 453 lines / 67 decision headings; 16,268 B / 101 lines. `crates/tungsten-core/tests/decision_index.rs` couples these paths. |
| `DESIGN.md`, `CHANGELOG.md` | 35,783 B / 328 lines; 88,572 B / 625 lines. Size alone does not make them startup context. |
| `docs/plans/phase4.md`, `perf-overhead-audit.md` | 26,084 B / 385 lines; 28,257 B / 220 lines. The latter is `done`; `physics-debug-audit.md` is `draft`. Fix completion is not inferred. |
| Manifests | Edition 2021, resolver 2, no workspace `rust-version`; four examples hard-code edition, three library crates inherit it. `stable` resolves locally to rustc 1.95.0. |
| Agent files | `.claude/` ignored; two local skills, one personal-preference guard hook. No project `.agents/`, `.codex/`, `.github/`, or `justfile`. |
| Review environment | Codex 0.155.0-alpha.16.3; Claude unavailable on PATH; nextest 0.9.133. just, cargo-deny/shear/machete and shellcheck unavailable on PATH. |
| Checks/scripts | 626 passed, four ignored, zero doctests. Two clippy warnings in `crates/tungsten-core/benches/physics_bench.rs`: `type_complexity`, `explicit_iter_loop`. Three scripts total **877 lines**, not 754. |
| WGSL | 67 paths across render sources and `assets/shaders/`, 36 distinct byte contents; **31 equal mirror pairs** (30 stock plus sprite). Seven named Naga tests cover SMAA/bloom, not every shader. |
| Visual test | `examples/02_sprite_stress/tests/visual_regression.rs` returns early without `TUNGSTEN_VISUAL_REGRESSION`. Its `tests/fixtures/baseline-sprite-stress.png` is absent; reference-machine fields are placeholders. Ordinary test success does not validate pixels. |

Root instructions plus the mandatory index read sum to 34,708 B for Claude and 32,257 B for Codex. These are document sums, **not observed context, token counts or tool calls**. The wgpu skill contradicts D-057/D-058 on shader hot reload; the perf skill's scene differs from the script default `ecs-high-load`. Both skills' `../../../` repo links resolve one directory too shallow. The proposed per-skill symlink itself resolves correctly but does not repair those body links.

## Agent setup constraints

- **Codex:** one instruction file per directory from repo root to launch CWD, preferring `AGENTS.override.md`, then `AGENTS.md`, then configured fallbacks; default combined project-doc budget 32 KiB. Root launch does not preload every descendant. Explicitly require reading applicable nested instructions before edits. [Discovery][codex-agents]
- **Claude:** native AGENTS loading starts at 2.1.277 and depends on competing ancestor/project CLAUDE files and settings. A sibling `CLAUDE.md` containing `@AGENTS.md` shares one canonical body across both tools, including older Claude clients. Nested instructions load when reading there. Keep `@` imports out of the shared body: Codex does not document expanding them. Claude's under-200-line guidance is a recommendation, not a hard limit. [Memory][claude-memory]
- **Skills:** Codex discovers `.agents/skills` up the CWD-to-root chain and follows symlinked skill folders; Claude uses `.claude/skills`. Share `name`/short `description` frontmatter. Claude's supported `paths` field is not a portable Codex activation contract. Verify symlink support on intended clones, especially Windows. [Codex][codex-skills], [Claude][claude-skills]
- **Ignore files:** `.ignore` controls ripgrep search hygiene; `.gitignore` controls Git/build noise. No `.claudeignore` contract was found in reviewed official docs. Claude Grep uses ripgrep; Glob needs `CLAUDE_CODE_GLOB_NO_IGNORE=false` to respect `.gitignore`. Test actual client behavior. Filters and prose are not access boundaries; never bypass the archive exclusion with `rg -uu`. [ripgrep][rg-guide], [Claude tools][claude-tools]
- **Permissions:** do not copy blanket cargo/just allows or personal network/deletion bans. Recipes/build scripts execute code. Codex project configuration/rules require trust; exec-policy rules govern execution outside the sandbox, not a universal push prohibition. Document sandbox and approval choices separately, respecting user/managed overrides. Suggested interactive Codex launch: `--sandbox workspace-write --ask-for-approval on-request`; downloads and protected `.agents`/`.codex` writes may need permission. Claude Read rules and Bash sandbox restrictions differ; do not deny build-tool access to `target/`. [Config][codex-config], [security][codex-security], [rules][codex-rules], [Claude permissions][claude-permissions], [sandbox][claude-sandbox]
- **Hooks/subagents:** both tools support project subagents and hooks. Schemas/edit payloads differ; Codex non-managed hooks require trust in their current definition. Post-edit hooks cannot undo edits; standalone rustfmt risks wrong edition/module scope. Defer hooks/custom subagents: explicit command gates offer a smaller shared workflow. [Codex hooks][codex-hooks], [subagents][codex-subagents], [Claude hooks][claude-hooks], [subagents][claude-subagents]

## Dependency and tool targets

Checked against crates.io APIs on 2026-09-24–25, excluding yanked/prerelease versions. Latest-column links are queried endpoints. Recheck at execution start and report changed targets. Requirements are ranges; the lockfile records exact resolution. Do not force transitive duplicates away with incompatible pins.

| Dependency | Current requirement → lock | Latest stable | Migration consideration |
| --- | --- | --- | --- |
| wgpu | 29.0.1 → 29.0.1 | [30.0.1](https://crates.io/api/v1/crates/wgpu) | Pair with glyphon; S9. Declared MSRV 1.87 is not the whole graph's MSRV. |
| glyphon | git `5ac0224…` → 0.11.0 | [0.12.0](https://crates.io/api/v1/crates/glyphon) | [0.11](https://crates.io/api/v1/crates/glyphon/0.11.0/dependencies) requires wgpu 29/cosmic-text 0.18; [0.12](https://crates.io/api/v1/crates/glyphon/0.12.0/dependencies) requires wgpu 30/cosmic-text 0.19. |
| cosmic-text (transitive) | 0.18.2 | [0.19.0](https://crates.io/api/v1/crates/cosmic-text) | Declared MSRV 1.89; verify text shaping/layout. |
| winit | 0.30.12 → 0.30.13 | [0.30.13](https://crates.io/api/v1/crates/winit) | Hold; 0.31.0-beta.3 is prerelease. No predicted duplicate-removal promises. |
| cpal | 0.15 → 0.15.3 | [0.18.2](https://crates.io/api/v1/crates/cpal) | Medium cost: device naming, config, sample rate/errors; S8. |
| symphonia | 0.5 → 0.5.5 | [0.6.1](https://crates.io/api/v1/crates/symphonia) | High cost: rewritten decode APIs; protect PCM/error behavior first. |
| notify | 6 → 6.1.1 | [8.2.0](https://crates.io/api/v1/crates/notify) | Event types moved; channel feature changes. Keep std mpsc/feature intent; defer 9.0.0-rc.5. [Changes][notify-changes] |
| glam | 0.32.1 → 0.32.1 | [0.33.10](https://crates.io/api/v1/crates/glam) | Camera constructors deprecated; numerical/SIMD changes affect comparisons. Keep default type features. [Changes][glam-changes] |
| pollster | 0.4 → 0.4.0 | [1.0.1](https://crates.io/api/v1/crates/pollster) | `FutureExt` now covers `IntoFuture`; verify existing `block_on` use. A changelog exists. [Changes][pollster-changes] |
| rtrb | 0.3 → 0.3.3 | [0.3.5](https://crates.io/api/v1/crates/rtrb) | Compatible refresh; **0.4.0 was not found**. |
| criterion (dev) | 0.5 → 0.5.1 | [0.8.2](https://crates.io/api/v1/crates/criterion) | MSRV 1.86; replace deprecated black_box imports. Measurement changes require new bench baselines. [Changes][criterion-changes] |
| serde | 1 → 1.0.228 | [1.0.229](https://crates.io/api/v1/crates/serde) | Compatible refresh; retain derive. |
| serde_json | 1 → 1.0.149 | [1.0.151](https://crates.io/api/v1/crates/serde_json) | Compatible refresh; manifest/config tests. |
| anyhow | 1 → 1.0.102 | [1.0.104](https://crates.io/api/v1/crates/anyhow) | Compatible refresh. |
| thiserror | 2 → 2.0.18 | [2.0.21](https://crates.io/api/v1/crates/thiserror) | Compatible refresh; transitive 1.0.69 also exists. |
| log | 0.4 → 0.4.29 | [0.4.34](https://crates.io/api/v1/crates/log) | Compatible refresh; parser check. |
| env_logger | 0.11 → 0.11.10 | [0.11.11](https://crates.io/api/v1/crates/env_logger) | Compatible refresh; preserve telemetry parsing. |
| bytemuck | 1 → 1.25.0 | [1.25.2](https://crates.io/api/v1/crates/bytemuck) | Compatible refresh; retain derive/layout checks. |
| image | 0.25 → 0.25.10 | [0.25.10](https://crates.io/api/v1/crates/image) | Current; PNG-only features; declared MSRV 1.88. |

Rust **1.98.1** is the latest stable listed by the [release index][rust-releases]; **2024** is the latest stable edition, introduced in 1.85. Pin the tested compiler; MSRV is a separate support promise. [Edition guide][edition]

Selected tools: [just 1.58.0](https://crates.io/api/v1/crates/just), [cargo-deny 0.20.2](https://crates.io/api/v1/crates/cargo-deny), [ShellCheck 0.11.0](https://github.com/koalaman/shellcheck/releases/tag/v0.11.0); optional [nextest 0.9.146](https://crates.io/api/v1/crates/cargo-nextest), declared build MSRV 1.91. Pin install versions and verify checksums; don't assume distro package versions. Nextest adoption requires test-inventory comparison, not an assumed speedup. [Test runner][nextest]

Rechecked but deferred: [cargo-shear 1.14.0](https://crates.io/api/v1/crates/cargo-shear), MSRV 1.95, and [cargo-machete 0.9.2](https://crates.io/api/v1/crates/cargo-machete); another mandatory gate is unnecessary. Defer linker/cache experiments, xtask, shader single-sourcing, WESL, HDR, pipeline caching and specialization until measured demand. Keep the default linker: Rust uses LLD for `x86_64-unknown-linux-gnu` from 1.90; no repo-specific improvement was measured. [Announcement][lld]

## Files to touch during implementation

- **Instructions/docs:** `AGENTS.md`, `CLAUDE.md`, `docs/LLM_INDEX.md`, `docs/DECISION_INDEX.md`, `docs/plans/README.md`; new `crates/tungsten-render/{AGENTS,CLAUDE}.md`, `docs/agent-setup.md`. Asset rules remain global, including example-local assets.
- **Agent setup:** `.gitignore`, new `.ignore`, `.claude/settings.json`, existing `.claude/skills/tungsten-{wgpu,perf}/SKILL.md`, new `.agents/skills/` symlinks. Remove `.claudeignore` after verification. Leave personal settings/guard and home directories alone.
- **Commands:** new `justfile`, `deny.toml`, `scripts/check-agent-context.py`, `scripts/test-smoke-examples.sh`, `.github/workflows/ci.yml`; existing three shell scripts and `docs/perf/profiling-workflow.md`; optional `.config/nextest.toml`.
- **Migrations:** `rust-toolchain.toml`, `.cargo/config.toml`, root/all seven member manifests, `Cargo.lock`, optional `rustfmt.toml`; edition-touched Rust; six benchmark files; umbrella audio/watcher, core decoder/tests/camera, render API call sites/shader tests. Decode fixtures live under tests with provenance; production assets still need manifests.
- **Verification/release:** visual fixture PNG/README under `examples/02_sprite_stress/tests/fixtures/`; append decisions, update their index and README/CHANGELOG to actual delivered scope; update this plan's progress.

Keep DECISIONS/DESIGN/CHANGELOG/index paths, shader mirrors and archived plans. No automatic deletion of captures or `target/`.

## Ordering and shared checks

| Step | Depends on | Parallelism / risk |
| --- | --- | --- |
| S0 baseline | none | Serial; hardware checks gate dependent migrations |
| S1 instructions/navigation | S0 | May overlap S3/S4/S6 with separate ownership; medium instruction risk |
| S2 skills/setup | S1 | Own agent/ignore files exclusively |
| S3 compiler | S0 | Serial with all Cargo/compiler-flag changes |
| S4 commands/scripts | S1, S2, S3 | Scripts may be drafted alongside docs; integrate/check afterward |
| S5 compatible deps/edition | S3, S4 | Serial, separate dependency/edition/format working-tree checkpoints |
| S6 shader coverage | S0 | May overlap docs/scripts; finish before S9 |
| S7 smaller upgrades | S5 | Serial, one dependency per working-tree checkpoint |
| S8 audio | S7 | Serial; decoder/device upgrades separate |
| S9 renderer/text | S6, S8 | Serial paired upgrade; highest GPU risk |
| S10 CI/integration | all | Scaffold after S4; final serial verification |

S0's inventory is sufficient to begin docs/tooling preparation; its hardware baseline must finish before compiler/runtime migrations. Parallel means independent files/checkouts, not simultaneous cargo fix, formatting, lockfile writes or benchmarks. Integrate/test checkpoints before proceeding. Undo a failed checkpoint by hand with its matching lockfile change; preserve unrelated work. Missing checks are not passes.

**CPU gate:** `cargo fmt --all -- --check`; `cargo test --workspace --locked`; `cargo clippy --workspace --all-targets --locked` (advisory until S4, then add `-- -D warnings`); `cargo bench --workspace --no-run --locked` for migrations. Use raw commands before recipes exist.

**GPU gate:** `WGPU_BACKEND=vulkan ./scripts/smoke-examples.sh`, plus `TUNGSTEN_VISUAL_REGRESSION=1 WGPU_BACKEND=vulkan cargo test -p example-02-sprite-stress --test visual_regression --locked -- --nocapture` on the reference machine. Existing smoke includes MSAA/depth-sort, post, SMAA, bloom and lighting fixtures. Other native targets need their own checks; Vulkan success is not Metal/DX12 certification.

**Perf comparison:** three sequential `WGPU_BACKEND=vulkan ./scripts/perf-capture.sh ecs-high-load 300 --telemetry-only` captures per checkpoint, same hardware/driver, pacing, scene, frame window and effective flags. Compare median run p95 total/acquire and reported render-stage means; retain logs. Investigate repeated >10% regression under the existing policy; account for noise/zero baselines. ECS timing cannot clear shader-specific regressions. Record compiler/dependency/flag changes separately.

## Ordered steps

### S0 — Recoverable baseline

Record revision, dirty/untracked files, host/tool versions and supported platforms. Use working-tree checkpoints on `0.27`; no archive/tag manipulation. Run CPU/GPU gates before migrations. Capture the missing PNG using the fixture README command **on the old code/toolchain**, record machine/driver provenance, then pass the visual test. Never generate a migrated baseline merely to pass. Record audible playback and perf baseline; preserve local artifacts.

**Done when:** CPU/smoke/visual/perf commands above succeed with logs; `test -s examples/02_sprite_stress/tests/fixtures/baseline-sprite-stress.png` succeeds; README provenance is real; audio playback is recorded. Missing hardware evidence blocks dependent migrations while docs/tooling can continue.

### S1 — Smaller instructions, stable doc paths

Read each doc fully before editing. Target root AGENTS ≤6,144 B and <200 lines, CLAUDE containing only `@AGENTS.md`, render AGENTS ≤4,096 B with its own one-line CLAUDE import, and LLM_INDEX ≤8,192 B. Preserve hard rules, asset-ID/manifest requirements, seam, test selection, archive prohibition and session scope. Remove volatile status and generic repetition; move detailed renderer guidance once. Don't add core/assets instruction files without unique scoped rules.

Condense the existing index in place without discarding useful non-redundant task mappings; make navigation on-demand. Tighten the decision index without losing IDs/its test. Keep DESIGN/DECISIONS/CHANGELOG on-demand with heading-scoped reads. Centralize plan conventions in `docs/plans/README.md`; defer other plans' status decisions. Append policy decisions when settled, allocating the then-next ID rather than reserving D-068/D-069.

**Done when:** `wc -lc AGENTS.md CLAUDE.md crates/tungsten-render/AGENTS.md docs/LLM_INDEX.md` meets budgets; `cargo test -p tungsten-core --test decision_index --locked` passes; index paths resolve. Fresh Claude `/context`/`/memory` and Codex instruction-summary prompts from root and render show root guidance once and applicable nested guidance. Record client versions and separate repo bytes from user/system context.

### S2 — Share existing skills and document controls

Replace blanket `.claude/` ignoring with an explicit allowlist for the two skills/project settings; keep personal state/hooks ignored. Review content before tracking it. Fix shader facts against D-057–D-061, canonical perf scene and links. Remove the assumed Context7 requirement; versioned docs.rs/upstream sources must work without that MCP server. Verify effective configuration because local/managed settings can affect project settings. [Settings precedence][claude-settings]

Expose `.agents/skills/<name> -> ../../.claude/skills/<name>` for each existing skill. Description ≤300 characters, body ≤8,192 B; add no skills. Check links through both access paths. Document Windows symlink prerequisites; resolve required-client incompatibility before calling sharing complete.

Add `.ignore` for archive/build/perf paths and loose profiler outputs; keep source, manifests, lockfile, fixtures and both shader trees searchable. Configure the documented Claude Glob option where supported. Retire `.claudeignore` after verifying behavior. Document sandbox/approvals/trust/nested discovery in `docs/agent-setup.md`; do not edit personal settings or install/update CLIs. Defer hooks, blanket execution allows and LSP plugins.

**Done when:** `git check-ignore .claude/settings.local.json` succeeds and project settings/skills are trackable; `test -f .agents/skills/tungsten-wgpu/SKILL.md` and the perf equivalent succeed. Both clients discover/invoke the two project skills with valid links. `rg --files --hidden -g '!.git/**' -g '!docs/plans/archive/**'` and Claude search omit generated paths while finding manifests/shaders. Inspect the archive exclusion, never scan the archive to test it. `jq -e . .claude/settings.json` passes; inspect effective permissions without push/delete probes.

### S3 — Compiler pin and honest support floor

Pin Rust 1.98.1 with minimal profile plus rustfmt/clippy; rust-analyzer is optional editor setup. Correct cargo-config comments. Keep CPU flags during comparison; no cargo clean. Compiler/lockfile pins don't freeze system libraries, drivers or environment flags. Document a generic CI `RUSTFLAGS` override: Cargo prioritizes it over target-config flags. [Cargo config][cargo-config]

Default proposal: workspace `rust-version = "1.98.1"`, inherited by all seven members, deliberately supporting the tested compiler. A lower MSRV requires separate agreement/testing; wgpu's 1.87 declaration is insufficient.

**Done when:** `rustc --version` reports 1.98.1; `cargo metadata --no-deps --format-version 1 --locked | jq -e 'all(.packages[]; .rust_version == "1.98.1")'` succeeds; CPU/GPU gates and a compiler-only perf comparison pass before dependency edits. Record new clippy findings without blanket suppression.

### S4 — Shared commands and reliable scripts

Add [just][just-manual] recipes: `fmt`, `fmt-check`, `lint`, `test`, `check`, `bench-build`, `smoke`, `visual`, `perf`, `perf-test`, `deps`, `ctx`; default lists recipes. `check` runs format-check, strict clippy and `cargo test --workspace --locked -q`, including doctests. Keep raw Cargo commands. Optional `test-fast` uses nextest failure output and always pairs with `cargo test --workspace --doc --locked`. Preserve exit codes/full failure diagnostics; no arbitrary output cap. Forward parameters as shell arguments without unsafe interpolation.

Fix baseline/new clippy warnings before enforcing strict lint. Configure cargo-deny from the actual graph, including notify's CC0-1.0 and Symphonia's MPL-2.0 license requirements; explain exceptions. Duplicates warn. Set `unknown-git = "deny"`, temporarily allow only glyphon's existing git source, then remove that exception in S9; an empty allow-list alone is not denial. No advance promise of a clean advisory result. [cargo-deny][deny-config]

Lint scripts; fix smoke discovery so bad metadata/jq or zero examples fails, and reconcile `timeout --preserve-status` with the current 124 check. Add stubbed shell regressions for discovery failure, timeout and child failure; preserve the fixture matrix. Don't blanket-add errexit to the sourced perf library: optional failures need explicit handling.

Keep the game's CWD at repo root for config/manifests when fixing profiler outputs. Prefer the existing capture wrapper with explicit output paths; verify the selected profiler's behavior. Current perf build sets `RUSTFLAGS="-C force-frame-pointers=yes"`, overriding native flags; make effective flags intentional/recorded and use matching pre/post captures. Rebaseline flag changes separately. No pruning or forced cleanup.

Add a standard-library-only `scripts/check-agent-context.py`: enforce S1/S2 budgets, check explicit navigation/skill links and symlinks, print root/render repo-byte totals. Never recurse into archives or label bytes exact tokens. Adopt <500-token plan digests, not a blanket size gate on existing active plans.

**Done when:** `just --list`, `just check`, `just bench-build`, `just deps`, `just ctx`, `just perf-test`, `bash scripts/test-smoke-examples.sh`, `shellcheck scripts/*.sh` pass; `just smoke` preserves the matrix. If the baseline has an advisory requiring a later upgrade, record its ID and temporary exception rather than blocking the upgrade on a circular gate; remove resolved exceptions by S10. Oversize/broken-link disposable fixtures fail the context checker. Check recipe argument/failure forwarding. If adopting nextest, compare test inventory/skips and run doctests. Profiler artifacts stay in their capture directory and recorded flags match the build.

### S5 — Compatible refresh, then edition

Refresh compatible dependencies, including rtrb 0.3.5; review every lock/source change and validate separately. At a clean checkpoint run `cargo fix --edition --workspace --all-targets` on 2021, then set workspace edition 2024/resolver 3 and make each example inherit edition. Review RPIT capture, temporary drop order, macros and cfg-specific code. Existing env mutation in `crates/tungsten-core/tests/display.rs` already uses unsafe blocks: audit safety, not merely compilation; a test-local lock doesn't cover arbitrary environment readers. [Migration][edition-migration], [unsafe changes][edition-unsafe], [inheritance][cargo-workspaces]

Separate mechanical formatting. Cargo fmt uses the manifest's style edition; document editor parity, or add `rustfmt.toml` with `style_edition = "2024"` if an editor invokes rustfmt directly. Resolver 3 changes MSRV-aware resolution; inspect its lockfile effects. [Style edition][style-edition], [resolver][resolver]

**Done when:** `cargo metadata --no-deps --format-version 1 --locked | jq -e 'all(.packages[]; .edition == "2024")'` succeeds; `just check`, `just bench-build`, GPU gate and perf comparison pass per checkpoint. Review supported targets and unresolved migration warnings; no unexplained unsafe migration TODO remains.

### S6 — Shader coverage before wgpu

Add a dependency-free integration test rooted via `CARGO_MANIFEST_DIR`: discover WGSL under the two known trees, run `validate_wgsl_source`, verify stock/sprite pairs bidirectionally, fail on empty discovery/missing pairs. Preserve specialized semantic/layout tests; remove only redundant assertions. Keep mirror layout.

**Done when:** `cargo test -p tungsten-render --locked` passes and reports 67 paths / 36 distinct contents / 31 pairs, or explains changes. Malformed WGSL and missing/mismatched mirrors each fail in a disposable copy. Naga success alone does not establish GPU/pixel correctness.

### S7 — Smaller upgrades separately

Upgrade notify 8.2.0, pollster 1.0.1, criterion 0.8.2, then glam 0.33.10, one working-tree checkpoint each. Retain std mpsc; verify file-watch save/recovery behavior. Replace six bench black_box imports. Migrate deprecated glam camera constructors in core camera/render renderer, preserving handedness/depth conventions. Retain type features; numerical changes need physics/image checks.

**Done when, per dependency:** `just check`, `just bench-build`, GPU gate pass. Notify: run an example, verify registered asset/shader reload, invalid-content recovery and rename-save, then restore edits. Glam: `cargo test --release -p tungsten-core --test physics_determinism --test physics_tunneling --test physics_containment --locked`, visual gate and perf comparison pass. Criterion: record fresh baselines; don't present harness changes as engine speedups.

### S8 — Decode and device migrations

Before Symphonia changes, add licensed/minimal fixtures and tests for enabled ogg/vorbis, wav, mp3 and AAC paths: rate/channels, interleaving, length/finite PCM, corrupt/truncated data. Current audio tests cover the registry, not decoding. Port to 0.6.1: optional typed codec parameters, removed SampleBuffer, new copy APIs, `Ok(None)` EOF. Distinguish actual I/O failures from EOF and retain an explicit decode-warning policy. [Migration][symphonia-migration]

Separately upgrade cpal 0.18.2: replace removed `Device::name()` with description API, remove sample-rate `.0`, pass config by value, handle unified errors. Keep existing `.play()`; `.start()` is **unreleased** in the reviewed changelog. PipeWire/PulseAudio priority applies only to enabled hosts; default features enable neither. Preserve backend selection rather than adding system dependencies unintentionally. [Changes][cpal-changes], [features][cpal-features], [device API][cpal-device]

**Done when:** decoder tests pass before/after; `just check`, `just bench-build`, `just deps`, GPU gate and perf comparison pass after each upgrade. `cargo run -p example-01-platformer`: exercise audible sound, volume, looping and stop; record host/device/errors and missing-device behavior. Silent initialization failure isn't playback success; record untested platforms.

### S9 — wgpu/glyphon in lockstep

Switch directly from git glyphon to registry 0.12.0 with wgpu 30.0.1, letting glyphon resolve cosmic-text 0.19. Avoid the redundant intermediate glyphon 0.11 migration. Review features/text output. Port queue-owned presentation, optional vertex slots, fallible mapped ranges, slice/binding conversions and explicit flat integer varyings. Add **`color_space: SurfaceColorSpace::Auto`** to preserve SDR behavior even while HDR is deferred. Check changed attachment validation/naga APIs; don't mechanically change every `as_entire_binding`. [Release notes][wgpu-changes]

Current acquire logic groups Lost with Outdated and accepts Suboptimal without later reconfiguration. Follow the versioned contract: distinguish surface recreation/reconfiguration, skip timeout/occlusion, bound retries, release acquired textures before configuring. Keep full device recovery out of scope unless required; unsupported recovery must fail clearly. Add focused transition tests and manual resize/minimize/restore. [Surface API][wgpu-surface]

**Done when:** `just check`, `just bench-build`, `just deps`, shader coverage, GPU gate and perf comparison pass; `! rg -q 'git\+' Cargo.lock` succeeds; `cargo tree --locked -i wgpu` resolves the intended single version. Verify text overlay, post/SMAA/bloom/lighting, screenshots and shader reload. Exercise GPU timing readback separately from perf captures. Explain pixel differences without silently replacing goldens. Record Metal/DX12 results or outstanding coverage.

### S10 — CI and final integration

Add CPU-only GitHub Actions for PRs, manual dispatch and pushes including `0.27`. Pin actions to reviewed SHAs and installers to checked releases. Reviewed setup action v2.0.0 reads the toolchain file and defaults to denied build warnings; align with local gates. Use minimal token permissions, superseded-run cancellation, and cache keys covering compiler/lockfile/flags. Start Linux prerequisites with pkg-config/ALSA/udev; a clean run establishes the complete list. [Setup action][setup-action]

Run shared CPU/dependency/context/script checks and bench compilation. Keep GPU/audio checks explicit/local; no performance thresholds on shared runners. Document installation and generic CPU override. No branch-protection changes or mandatory PR process.

**Done when:** fresh CI checkout is green at final revision; local CPU/GPU/audio/perf and both client setup checks pass. Append policy rationale without rewriting settled decisions; update README/CHANGELOG to delivered scope. `git diff --check` and `git status --short` show only intended implementation changes. Mark done only after required checks; leave archival to the owner under the existing convention.

## Final acceptance

**Status 2026-09-25:** items 1–3 are satisfied on the reference machine; item 4 is partly outstanding.

- Outstanding: **CI green** on the final revision (needs the owner's push; local CI simulation passed). **Metal/DX12** rendering and **macOS CoreAudio / Windows WASAPI** audio after the wgpu 30 / cpal 0.18 upgrades (no hosts; Vulkan/ALSA success is not certification). **Windows skill symlinks** (documented, untested).
- Scope notes for item 4: `docs/plans/archive/` was never read, listed or scanned; one early ad hoc S1 index check `stat()`ed the directory path itself, and `check-agent-context.py` skips archive paths. Personal/home changes, all user-approved or reverted: `just`/`cargo-deny` in `~/.cargo/bin`, ShellCheck in `~/.local/bin`, Rust 1.98.1 toolchain; the rustup global default was set by the installer and reverted to none; Codex CLI upgraded 0.120.0 → 0.157.0 per user instruction, with `~/.codex/config.toml` edited temporarily and restored byte for byte. No forced cleanup: pre-existing root `perf.data`/`perf.data.old`/`flamegraph.svg` and all captures were left, and `target/ci-sim` (CI simulation) remains for the owner to remove. No engine features and no speedup claims: perf deltas are recorded as variance, and the confounded S9 set was re-run.


1. Every S0–S10 check has revision/environment/command/result recorded; unavailable checks remain outstanding.
2. Instruction budgets pass; shared guidance/skills load without duplicated bodies. Repo byte totals aren't total session context or token savings.
3. CPU/dependency/scripts/full smoke/visual/audio/relevant perf checks pass; the lockfile has no git source; every package reports edition 2024 and the chosen rust-version.
4. CI is green on the final revision. No archive access/deletion, personal config changes, forced cleanup, speculative engine features or unsupported performance claims.

## Sources

Retrieved **2026-09-24–25**; version APIs are linked above. Recheck moving documentation when executing, especially client compatibility.

[codex-agents]: https://learn.chatgpt.com/docs/agent-configuration/agents-md
[claude-memory]: https://code.claude.com/docs/en/memory
[codex-skills]: https://learn.chatgpt.com/docs/build-skills
[claude-skills]: https://code.claude.com/docs/en/skills
[rg-guide]: https://github.com/BurntSushi/ripgrep/blob/master/GUIDE.md
[claude-tools]: https://code.claude.com/docs/en/tools-reference
[codex-config]: https://learn.chatgpt.com/docs/config-file/config-basic
[codex-security]: https://learn.chatgpt.com/docs/agent-approvals-security
[codex-rules]: https://learn.chatgpt.com/docs/agent-configuration/rules
[claude-permissions]: https://code.claude.com/docs/en/permissions
[claude-sandbox]: https://code.claude.com/docs/en/sandboxing
[codex-hooks]: https://learn.chatgpt.com/docs/hooks
[codex-subagents]: https://learn.chatgpt.com/docs/agent-configuration/subagents
[claude-hooks]: https://code.claude.com/docs/en/hooks
[claude-subagents]: https://code.claude.com/docs/en/sub-agents
[claude-settings]: https://code.claude.com/docs/en/settings
[notify-changes]: https://github.com/notify-rs/notify/blob/main/notify/CHANGELOG.md
[glam-changes]: https://github.com/bitshifter/glam-rs/blob/main/CHANGELOG.md
[pollster-changes]: https://github.com/zesterer/pollster/blob/master/CHANGELOG.md
[criterion-changes]: https://github.com/criterion-rs/criterion.rs/blob/master/CHANGELOG.md
[rust-releases]: https://blog.rust-lang.org/releases/
[edition]: https://doc.rust-lang.org/edition-guide/rust-2024/index.html
[edition-migration]: https://doc.rust-lang.org/edition-guide/editions/transitioning-an-existing-project-to-a-new-edition.html
[edition-unsafe]: https://doc.rust-lang.org/edition-guide/rust-2024/newly-unsafe-functions.html
[resolver]: https://doc.rust-lang.org/edition-guide/rust-2024/cargo-resolver.html
[style-edition]: https://doc.rust-lang.org/edition-guide/rust-2024/rustfmt-style-edition.html
[cargo-workspaces]: https://doc.rust-lang.org/cargo/reference/workspaces.html
[cargo-config]: https://doc.rust-lang.org/cargo/reference/config.html
[nextest]: https://nexte.st/docs/running/
[lld]: https://blog.rust-lang.org/2025/09/01/rust-lld-on-1.90.0-stable/
[just-manual]: https://just.systems/man/en/
[deny-config]: https://embarkstudios.github.io/cargo-deny/checks/cfg.html
[symphonia-migration]: https://github.com/pdeljanov/Symphonia/blob/main/docs/guides/migration/0p6.md
[cpal-changes]: https://github.com/RustAudio/cpal/blob/master/CHANGELOG.md
[cpal-features]: https://crates.io/api/v1/crates/cpal/0.18.2
[cpal-device]: https://docs.rs/cpal/0.18.2/cpal/traits/trait.DeviceTrait.html
[wgpu-changes]: https://github.com/gfx-rs/wgpu/releases/tag/v30.0.0
[wgpu-surface]: https://docs.rs/wgpu/30.0.1/wgpu/enum.CurrentSurfaceTexture.html
[setup-action]: https://github.com/actions-rust-lang/setup-rust-toolchain

## Review notes (Codex)

**Material changes and reasons:**

- Replaced overlapping A/B/C waves with checkpoints/shared gates; removed circular recipe prerequisites and conflicting parallel ownership.
- Reframed context bytes as file sums; removed unsupported token/call/timing claims, universal speedups and arbitrary search/output quotas. Corrected scripts to 877 lines and shader mirrors to 31 pairs.
- Preserved doc paths/history/archive: compact instructions/navigation cost less than 67 decision splits and mass relinking. Deferred extra skills/subagents/hooks/LSP, cache/linker work and artifact pruning.
- Corrected both clients' discovery, Claude native AGENTS support, portable skill activation and broken links. Removed false Codex subagent limitation and universal ignore/prefix enforcement claims.
- Rechecked dependencies/tools: rtrb 0.3.5, pollster changelog, paired glyphon/wgpu; retained stable winit/notify. Added example editions/inherited rust-version, numerical/benchmark costs, cpal naming/features and required wgpu color-space migration.
- Found missing visual reference/env-gated test; baseline now precedes upgrades. Added decode/AAC/error coverage, hardware verification, flags-consistent comparisons, smoke discovery/timeout handling and profiler CWD constraints.
- Made CI/source denial/checks concrete; retained doctests and failure diagnostics. Status remains `draft`; no plan step executed.

**Unverified/removed claims:** Archive sizes/counts/hit percentages were not checked because reading the archive is prohibited. Personal-home context sizes, RTK behavior, startup totals and nextest timing/output claims were not reproduced. Original Claude 2.1.110 / Codex 0.120.0 claims do not describe this environment. GPU/audio correctness, migration success, CI packages, lower MSRV, Windows skill symlinks and effective client loading remain implementation checks. No guaranteed duplicate/performance improvement or byte-reproducible-build claim remains.

**Open questions for the user:**

1. Is supporting only pinned Rust 1.98.1 acceptable, and which native platforms must pass? Proposed default: Linux/Vulkan required; additional claimed platforms need validation.
2. Which machine supplies visual/audio/perf baselines, and which Claude client version must load shared skills? This review's CPU results cannot replace those checks.
3. Confirm CPU-only CI as the intended reversal of current no-CI policy; proposed default: no branch-protection changes. Preserve archive and defer large doc splits.

## Follow-ups (logged, not implemented)

- Enable Symphonia's `pcm` feature so PCM WAV decodes; `tests/audio_decode.rs` pins today's "unsupported codec" error (pre-existing gap found in S8).
- Truncated Ogg files fail at probe (the reader scans for the final page); decide whether to decode the available prefix, as MP3 does.
- Evaluate rtrb 0.4.0 (published 2026-08-17) against 0.3.5.
- Drop the RUSTSEC-2026-0192 exception when cosmic-text/fontdb stop using `ttf-parser`.
- Adopt winit 0.31 once it leaves prerelease.
- Re-verify instruction loading when Claude Code reaches native `AGENTS.md` support (2.1.277+), since the `CLAUDE.md` import could then double-load.
- `perf-capture.sh` creates its output directory before validating the scene name, leaving an empty directory on typos.
- Only `example-01-platformer` enables hot reload; the shader playground could enable it for shader work.
- Refresh stale release-status wording (README/DESIGN branch lines; README's "uniform-grid broad-phase" predates D-062) at the next release.
- Add `actionlint` to the script checks if the workflow grows.

